server.port=${PORT:8080}
server.address=127.0.0.1
spring.main.banner-mode=off

linklock.db-path=${DB_PATH:linklock.db}
linklock.email.latency-ms=${EMAIL_LATENCY_MS:300}
linklock.email.failure-rate=${EMAIL_FAILURE_RATE:0.05}
linklock.email.base-url=${BASE_URL:http://localhost:8080}
