server.port=${PORT:8080}
server.address=127.0.0.1
spring.main.banner-mode=off

transcribe.db-path=${DB_PATH:transcribe.db}
transcribe.transcription.latency-ms=${TRANSCRIPTION_LATENCY_MS:500}
transcribe.transcription.failure-rate=${TRANSCRIPTION_FAILURE_RATE:0.10}
