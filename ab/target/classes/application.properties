server.port=${PORT:8080}
server.address=127.0.0.1
spring.main.banner-mode=off

schedulr.db-path=${DB_PATH:schedulr.db}
schedulr.email.latency-ms=${EMAIL_LATENCY_MS:300}
schedulr.email.failure-rate=${EMAIL_FAILURE_RATE:0.05}
